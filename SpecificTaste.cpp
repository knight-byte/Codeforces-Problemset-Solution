#include <bits/stdc++.h>
using namespace std;

int main(void) {
    int t;
    cin >> t;
    while (t--) {
        int n;
        cin >> n;
        vector<int> a;
        for (int i=0; i<n; i++)
            a.push_back(1); // what the fuck
        for (auto &x:a)
            cout << x << " ";
        cout << endl;
    }

    return 0;
}