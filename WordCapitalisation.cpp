#include <bits/stdc++.h>
using namespace std;

int main(void) {
    string word;
    cin >> word;
    // Do I need to explain this, cuh?
    word[0] = toupper(word[0]);
    cout << word << endl;

    return 0;
}