#include <bits/stdc++.h>
using namespace std;

int main(void) {
    int t;
    cin >> t;
    while (t--) {
        int l, r;
        cin >> l >> r;
        cout << l << " " << l*2 << endl;
    }

    return 0;
}